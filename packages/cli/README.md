# @skillrecordings/cli

CLI for the support platform (skill init, setup tasks).

## Purpose
- Project scaffolding via `skill init`
- Local developer workflows

## Key paths
- `packages/cli/src/`

## Do / Don’t
- Do keep CLI output deterministic for automation
- Don’t add interactive prompts without a non-interactive flag
