# Local Eval Environment Configuration
# Copy to .env.eval and adjust as needed

# Database (local MySQL via Docker)
DATABASE_URL=mysql://eval_user:eval_pass@localhost:3306/support_eval

# Redis (local via Docker)
REDIS_URL=redis://localhost:6379

# Vector Store (Qdrant via Docker - mimics Upstash Vector API structure)
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=support_eval

# Embeddings (Ollama - use host Ollama if available)
OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_MODEL=mxbai-embed-large

# LLM (still uses real Anthropic API for agent responses)
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Eval mode flag
EVAL_MODE=local

# Disable external services in eval mode
FRONT_API_KEY=
SLACK_WEBHOOK_URL=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
