---
"@skillrecordings/cli": minor
---

Add `skill front search` with full Front query syntax support — text search, inline filters (inbox, tag, from, to, assignee, status, date ranges, custom fields), pagination, and comprehensive `--help` cheat sheet. Fix `skill -V` to read version from package.json instead of hardcoded 0.0.0.
