---
"@skillrecordings/cli": minor
---

Add `skill front search` command with full Front query syntax (text, inbox, tag, assignee, status, sender, date range). Fix `skill -V` to read version from package.json instead of hardcoded 0.0.0.
